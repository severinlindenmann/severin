---
type: page
title: About
date: 2025-01-01
---

import Image from "next/image";
import { TranslatedPage } from "../components/TranslatedPage";
import EmailCopy from "../components/EmailCopy";

<TranslatedPage>
{(t) => (
  <>
    <header className="profile-intro">
      <div className="profile-intro__media">
        <Image
          src="https://storage.googleapis.com/seli-public-bucket-001/images/me2022.webp"
          alt="Severin Lindenmann"
          width={1200}
          height={800}
          priority
          className="profile-intro__photo"
        />
      </div>
      <div className="profile-intro__content">
        <p className="profile-intro__kicker">{t('about.kicker')}</p>
        <h1>{t('about.greeting')}</h1>
        <p className="profile-intro__summary">{t('about.intro1')}</p>
        <p className="profile-intro__summary">{t('about.intro2')}</p>
        <ul className="profile-intro__links">
          <li><a href="https://github.com/severinlindenmann" target="_blank" rel="noreferrer">GitHub</a></li>
          <li><a href="https://www.linkedin.com/in/severin-lindenmann/" target="_blank" rel="noreferrer">LinkedIn</a></li>
          <li><EmailCopy /></li>
          <li><a href="https://wa.me/41765613150" target="_blank" rel="noreferrer">WhatsApp</a></li>
        </ul>
      </div>
    </header>

    <section className="profile-sections">
      <article className="profile-section">
        <h2>{t('about.whatImDoing')}</h2>
        <p>
          {t('about.currentRole')}{" "}
          <a href="https://altyca.ch/de/" target="_blank" rel="noreferrer">ALTYCA</a>
          {" "}{t('about.since')}
        </p>
        <ul>
          {t('about.responsibilities').map((item, index) => (
            <li key={index}>{item}</li>
          ))}
        </ul>
      </article>

      <article className="profile-section">
        <h2>{t('about.whereIveBeen')}</h2>
        <p>{t('about.previousRoles')}</p>
        <ul className="profile-timeline">
          <li><strong>{t('about.experiences.0.role')}</strong><a href="https://www.btelligent.com/" target="_blank" rel="noreferrer">{t('about.experiences.0.company')}</a><strong>: </strong>{t('about.experiences.0.description')}</li>
          <li><strong>{t('about.experiences.1.role')}</strong><a href="https://voicy.ch/" target="_blank" rel="noreferrer">{t('about.experiences.1.company')}</a><strong>: </strong>{t('about.experiences.1.description')}</li>
          <li><strong>{t('about.experiences.2.role')}</strong><a href="https://layer.swiss/" target="_blank" rel="noreferrer">{t('about.experiences.2.company')}</a><strong>: </strong>{t('about.experiences.2.description')}</li>
          <li><strong>{t('about.experiences.3.role')}</strong>{t('about.experiences.3.description')}</li>
          <li><strong>{t('about.experiences.4.role')}</strong><a href="https://screenimage.ch/" target="_blank" rel="noreferrer">{t('about.experiences.4.company')}</a><strong>: </strong>{t('about.experiences.4.description')}</li>
        </ul>
      </article>

      <article className="profile-section">
        <h2>{t('about.education.title')}</h2>
        <ul className="education-list">
          <li>
            <strong>{t('about.education.bachelor.title')}</strong>
            <span className="education-list__meta">{t('about.education.bachelor.period')}</span>
            <p>{t('about.education.bachelor.institution')}</p>
          </li>
          <li>
            <strong>{t('about.education.berufsmatur.title')}</strong>
            <span className="education-list__meta">{t('about.education.berufsmatur.period')}</span>
            <p>{t('about.education.berufsmatur.institution')}</p>
          </li>
          <li>
            <strong>{t('about.education.apprentice.title')}</strong>
            <span className="education-list__meta">{t('about.education.apprentice.period')}</span>
            <p>{t('about.education.apprentice.institution')}</p>
          </li>
        </ul>
      </article>

      <article className="profile-section">
        <h2>{t('about.certifications.title')}</h2>
        <div className="certifications-grid">
          {t('about.certifications.items').map((cert, index) => {
            const iconMap = {
              'azure': '/images/azure.svg',
              'databricks': '/images/databricks.svg',
              'github': '/images/github.svg',
              'google': '/images/google.svg'
            };
            const iconSrc = cert.iconKey ? iconMap[cert.iconKey] : null;
            
            return (
              <div key={index} className="certification-badge">
                {iconSrc ? (
                  <div className="certification-badge__icon certification-badge__icon--image">
                    <Image src={iconSrc} alt={cert.issuer} width={48} height={48} />
                  </div>
                ) : (
                  <div className="certification-badge__icon">{cert.icon}</div>
                )}
                <div className="certification-badge__content">
                  <strong>{cert.name}</strong>
                  <span className="certification-badge__issuer">{cert.issuer}</span>
                  <span className="certification-badge__date">{cert.date}</span>
                </div>
              </div>
            );
          })}
        </div>
      </article>

      <article className="profile-section">
        <h2>{t('about.skills.title')}</h2>
        <div className="skills-grid">
          <div className="skill-category">
            <h3>{t('about.skills.dataEngineering.title')}</h3>
            <div className="skill-tags">
              {t('about.skills.dataEngineering.items').map((skill, index) => (
                <span key={index} className="skill-tag">{skill}</span>
              ))}
            </div>
          </div>
          <div className="skill-category">
            <h3>{t('about.skills.programming.title')}</h3>
            <div className="skill-tags">
              {t('about.skills.programming.items').map((skill, index) => (
                <span key={index} className="skill-tag">{skill}</span>
              ))}
            </div>
          </div>
          <div className="skill-category">
            <h3>{t('about.skills.devOps.title')}</h3>
            <div className="skill-tags">
              {t('about.skills.devOps.items').map((skill, index) => (
                <span key={index} className="skill-tag">{skill}</span>
              ))}
            </div>
          </div>
          <div className="skill-category">
            <h3>{t('about.skills.frameworks.title')}</h3>
            <div className="skill-tags">
              {t('about.skills.frameworks.items').map((skill, index) => (
                <span key={index} className="skill-tag">{skill}</span>
              ))}
            </div>
          </div>
        </div>
      </article>

      <article className="profile-section profile-section--highlight">
        <h2>{t('about.beyondResume')}</h2>
        <p>{t('about.beyondText1')}</p>
        <p>{t('about.beyondText2')}</p>
        <p>{t('about.beyondText3')}</p>
      </article>
    </section>

    <style jsx>{`
      .profile-intro {
        display: grid;
        grid-template-columns: 1fr 1.5fr;
        gap: 3rem;
        align-items: start;
        margin-bottom: 4rem;
        padding: 3rem;
        border-radius: 24px;
        border: 1px solid var(--surface-border);
        background: var(--surface);
        box-shadow: var(--surface-shadow);
        backdrop-filter: blur(18px);
      }

      .profile-intro__media {
        position: relative;
        border-radius: 16px;
        overflow: hidden;
      }

      .profile-intro__photo {
        width: 100%;
        height: auto;
        display: block;
      }

      .profile-intro__kicker {
        color: var(--accent);
        font-weight: 600;
        margin: 0 0 1rem;
      }

      .profile-intro h1 {
        font-size: 2.5rem;
        font-weight: 800;
        margin: 0 0 1.5rem;
      }

      .profile-intro__summary {
        color: var(--text-muted);
        line-height: 1.7;
        margin-bottom: 1.5rem;
      }

      .profile-intro__links {
        display: flex;
        gap: 1.5rem;
        list-style: none;
        padding: 0;
        margin: 2rem 0 0;
        flex-wrap: wrap;
      }

      .profile-intro__links a {
        color: var(--accent);
        font-weight: 600;
        text-decoration: none;
        transition: color 0.2s ease;
      }

      .profile-intro__links a:hover {
        color: var(--accent-strong);
      }

      .profile-sections {
        display: flex;
        flex-direction: column;
        gap: 2rem;
      }

      .profile-section {
        width: 100%;
        max-width: 100%;
        padding: 2.5rem;
        border-radius: 20px;
        border: 1px solid var(--surface-border);
        background: var(--surface);
        box-shadow: var(--surface-shadow);
        backdrop-filter: blur(18px);
        box-sizing: border-box;
      }

      .profile-section h2 {
        font-size: 1.75rem;
        font-weight: 700;
        margin: 0 0 1.5rem;
      }

      .profile-section p {
        color: var(--text-muted);
        line-height: 1.7;
        margin-bottom: 1rem;
      }

      .profile-section ul {
        color: var(--text-muted);
        line-height: 1.8;
      }

      .profile-section--highlight {
        background: linear-gradient(135deg, var(--surface), var(--background));
      }

      .profile-timeline {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .profile-timeline li {
        padding-left: 1.5rem;
        position: relative;
      }

      .profile-timeline li::before {
        content: "→";
        position: absolute;
        left: 0;
        color: var(--accent);
        font-weight: bold;
      }

      .profile-timeline a {
        color: var(--accent);
        text-decoration: none;
        margin: 0 0.25rem;
      }

      .profile-timeline a:hover {
        text-decoration: underline;
      }

      @media (max-width: 768px) {
        .profile-intro {
          grid-template-columns: 1fr;
          gap: 2rem;
          padding: 2rem;
        }

        .profile-intro h1 {
          font-size: 2rem;
        }

        .profile-section {
          padding: 1.5rem;
        }
      }

      .education-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
      }

      .education-list li {
        padding-bottom: 1.5rem;
        border-bottom: 1px solid var(--surface-border);
      }

      .education-list li:last-child {
        border-bottom: none;
        padding-bottom: 0;
      }

      .education-list strong {
        display: block;
        font-size: 1.125rem;
        margin-bottom: 0.25rem;
      }

      .education-list__meta {
        display: block;
        color: var(--accent);
        font-size: 0.95rem;
        margin-bottom: 0.5rem;
      }

      .education-list p {
        margin: 0;
        color: var(--text-muted);
      }

      .certifications-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
      }

      .certification-badge {
        display: flex;
        gap: 1rem;
        padding: 1.5rem;
        border-radius: 12px;
        border: 1px solid var(--surface-border);
        background: var(--background);
        transition: all 0.2s ease;
      }

      .certification-badge:hover {
        border-color: var(--accent);
        transform: translateY(-2px);
      }

      .certification-badge__icon {
        font-size: 2rem;
        flex-shrink: 0;
      }

      .certification-badge__icon--image {
        font-size: inherit;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .certification-badge__icon--image img {
        width: 48px;
        height: 48px;
        object-fit: contain;
      }

      .certification-badge__content {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
      }

      .certification-badge strong {
        color: var(--text);
        font-size: 1rem;
      }

      .certification-badge__issuer {
        color: var(--text-muted);
        font-size: 0.9rem;
      }

      .certification-badge__date {
        color: var(--accent);
        font-size: 0.85rem;
        font-weight: 600;
      }

      .skills-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
      }

      .skill-category h3 {
        font-size: 1.125rem;
        font-weight: 600;
        margin: 0 0 1rem;
        color: var(--accent);
      }

      .skill-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
      }

      .skill-tag {
        padding: 0.5rem 1rem;
        border-radius: 8px;
        background: var(--background);
        border: 1px solid var(--surface-border);
        font-size: 0.9rem;
        color: var(--text);
        transition: all 0.2s ease;
      }

      .skill-tag:hover {
        border-color: var(--accent);
        color: var(--accent);
      }
    `}</style>
  </>
)}
</TranslatedPage>
